# Increase Bluetooth power for better connection between halves
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Enable deep sleep to save battery (optional but recommended)
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000
